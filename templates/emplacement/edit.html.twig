{% extends 'base.html.twig' %}

{% block title %}Edit Emplacement{% endblock %}

{% block body %}
<div id="layoutSidenav_content">
<div>
    <h1>Modification Emplacement</h1>

    {{ include('emplacement/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <a href="{{ path('emplacement_index') }}" class="btn btn-secondary">retour à la liste</a>

    {{ include('emplacement/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
