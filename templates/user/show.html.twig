{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}


{% block body %}

    
<div id="layoutSidenav_content">
    <table class="table">
        <tbody>
            
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
                <td></td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>
                {% for role in user.roles %}
                    {% if role=="ROLE_ADMIN"%}
                        Admninistrateur
                    {% else %}
                        Magasinier
                    {% endif %}
                {% endfor %}
                </td>
                <td></td>
            </tr>
            
            <tr>
                <th>Nom</th>
                <td>{{ user.lastname }}</td>
                <td></td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.firstname }}</td>
                <td></td>
            </tr>
            <tr>
                <td><a href="{{ path('user_index') }}" class="btn btn-secondary">retour à la liste des utilisteurs</a></td>
                <td> <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-success">modification</a></td>
                <td>{{ include('user/_delete_form.html.twig') }}</td>
            </tr>
           
        </tbody>
    
        
    </table>
   

        
        
        
</div>  

    



   
{% endblock %}
